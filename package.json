{
  "name": "ft50-paper-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python -m http.server 8000 --bind 127.0.0.1",
    "test": "node --test"
  }
}
